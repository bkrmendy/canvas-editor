// time spent with cursor on (showing)
export const blinkOn = 600;

// time spent with cursor off (not showing)
export const blinkOff = 400;

// Editor canvas width in pixels
export const editorWidth = 500;
